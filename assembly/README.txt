Please note that not all of these tests will work.
Some of the assembly files were written with an old testbench was used, this would clock the values starting at 2 instead of 0, as a result these will load/store to an inccorect address.
You can still find simulations of these in the sim directory
