Mostly to test if the assmebler + tb works. Also tests jumps should loop and count forever

This comment at the top should not be registered as anything, comments can be done like this

addi 1 1 1 
sw 0 1 0
jalr 0 0 4
addi 0 0 0 This is a NOP, so no errors show up in the pipelining
addi 0 0 0 These comments should also not change anything and they don't
addi 0 0 0
addi 0 0 0

